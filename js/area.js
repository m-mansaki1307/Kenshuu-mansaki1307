/*
 * 四角の面積を計算してください。
 *
 * length: 縦の長さ
 * width: 横の長さ
 */
function getArea(length=1000 , width =1000) {
  let area = length * width;
  console.log(area);
  return area;
}


/**
* 四角の周囲を計算してください。
*
*
* length: 縦の長さ
* width: 横の長さ
*/
function getPerimeter(length=100, width=100) {
  let perimeter =length + width *2;
  console.log(Perimeter);
  return perimeter
 
}


// area.js を作成して以下コードを完成して
// それぞれのfunctionを呼び出して結果を
// console.logで表示してください。

// 条件

// length, width は1～1000の間ではないといけない。